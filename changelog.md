* Add new config options
  * `filterItems` Filter which items are durability checked
  * `itemFilter` If filterItems is enabled, any item that is not in this list will be ignored (Empty list = all item will be checked)
  * `soundCooldown` The amount of milliseconds between notification sound plays